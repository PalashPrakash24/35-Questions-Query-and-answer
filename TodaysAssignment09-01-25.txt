Sol 1 )SELECT emp_name, salary FROM Employees ORDER BY salary DESC;

Sol 2 )SELECT * FROM Employees ORDER BY salary DESC LIMIT 5;

Sol 3 ) SELECT * FROM employee
	LIMIT 3, 5;

Sol 4 ) SELECT dept_id, COUNT(*) AS employee_count
	FROM employee
 	GROUP BY dept_id;

Sol 5 ) SELECT dept_id, AVG(salary) AS avg_salary
	FROM employee
	GROUP BY dept_id
 	HAVING AVG(salary) > 50000;

Sol 6 ) SELECT * FROM Products
        WHERE product_name LIKE '%Laptop%';

Sol 7 ) SELECT SUBSTRING(student_name, 1, 3) AS first_three_chars
	FROM Students;

Sol 8 ) SELECT * FROM employee
	WHERE student_name LIKE 'A%';

Sol 9 ) SELECT MONTH(order_date) AS order_month, COUNT(*) AS total_orders
 	FROM employee
	GROUP BY MONTH(order_date)
	HAVING COUNT(*) > 100;

Sol 10 )SELECT category,
       SUM(sales_amount) AS total_sales,
       AVG(sales_amount) AS avg_sales
       FROM employee
       GROUP BY category
       HAVING SUM(sales_amount) > 100000;

Sol 11 ) SELECT * FROM employee
	 WHERE salary BETWEEN 30000 AND 60000;


Sol 12 )SELECT * FROM employee
 	WHERE department NOT IN ('HR', 'Admin');
	
Sol 13 )SELECT * FROM student
	WHERE email_id IS NULL;

Sol 14 )SELECT * FROM employee
        WHERE dept_id IN (1, 3);

Sol 15 ) SELECT * FROM employee
 	 WHERE age > 25
  	 AND salary < 50000;

Sol 16 ) SELECT dept_id, MAX(salary) AS max_salary
	FROM employee
	GROUP BY dept_id;

Sol 17 ) SELECT city, COUNT(*) AS student_count
	FROM student
	GROUP BY city;

Sol 18 ) SELECT MIN(salary) AS min_salary,
         MAX(salary) AS max_salary,
         AVG(salary) AS avg_salary
         FROM employee;
Sol 19 )SELECT dept_id, COUNT(*) AS employee_count
	FROM employee
	GROUP BY dept_id
	HAVING COUNT(*) > 5;

Sol 20 ) SELECT dept_id, SUM(salary) AS total_salary
	FROM employee
	GROUP BY dept_id
	ORDER BY total_salary DESC;

Sol 21 ) SELECT UPPER(emp_name) AS employee_name_uppercase
	 FROM employee;

Sol 22 ) SELECT emp_name, LENGTH(emp_name) AS name_length
	 FROM employee;

Sol 23 ) SELECT RIGHT(student_name, 2) AS last_two_chars
	 FROM student;

Sol 24 ) SELECT * FROM employee
	ORDER BY salary LIMIT 3;

Sol 25 ) SELECT * FROM products
	 ORDER BY price ASC, product_name DESC;

Sol 26 ) C) HAVING
Sol 27 ) A) COUNT()
Sol 28 ) A) IS NULL	
Sol 29 ) B) ORDER BY
Sol 30 ) B) Check a range of values	
Sol 31 ) C) AVG()
Sol 32 ) B) GROUP BY
Sol 33 ) C) LIMIT
Sol 34 ) C) LIKE
Sol 35 ) A) Count of non-NULL values




